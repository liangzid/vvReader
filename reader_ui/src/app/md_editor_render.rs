use egui;

pub use easy_mark_editor::EasyMarkEditor;

pub fn render_md_editor(ui:&egui::Ui,
			md:&mut (bool,String,EasyMarkEditor),
){
    if md.1==""{
	editor.ui(ui);
    }
    else{ // their exist the text file.

	// not here.
	// todo!
	// 1. first, load the file

	// 2. then, load the editor
	
	
    }

}


